[build]
  command = "npm run build"
  publish = "dist/portfolio-website/browser"

[functions]
  directory = "dist/portfolio-website/server"
  node_bundler = "esbuild"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/main"  # <-- This is the corrected line
  status = 200